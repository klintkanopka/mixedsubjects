# Compute confidence interval

Compute confidence interval

## Usage

``` r
compute_ci(estimate, se, conf_level = 0.95)
```

## Arguments

- estimate:

  Point estimate

- se:

  Standard error

- conf_level:

  Confidence level (default 0.95)

## Value

Named vector with ci_lower and ci_upper
