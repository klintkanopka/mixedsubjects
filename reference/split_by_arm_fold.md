# Split data by arm and fold

Split data by arm and fold

## Usage

``` r
split_by_arm_fold(data, n_folds = 2, seed = NULL)
```

## Arguments

- data:

  An msd_data object

- n_folds:

  Number of folds

- seed:

  Random seed

## Value

A list with fold assignments for each arm
