# Compute sample variance (population formula, divide by n)

Compute sample variance (population formula, divide by n)

## Usage

``` r
var_pop(x)
```

## Arguments

- x:

  A numeric vector

## Value

Sample variance using n denominator
